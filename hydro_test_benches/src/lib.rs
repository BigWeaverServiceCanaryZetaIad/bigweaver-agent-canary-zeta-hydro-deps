// Benchmarks for hydro_test that depend on timely and differential-dataflow
// These have been moved to this separate repository to avoid polluting the main
// repository with these dependencies.

pub mod paxos_bench;
pub mod two_pc_bench;
